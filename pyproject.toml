[project]
name = "Planet Map Generator"
version = "1.0.3"
description = "A tool to get fantasy maps from Torben Mogensen's generator and visualize them on a globe"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "streamlit>=1.60.0",
]

[tool.uv]
package = false
